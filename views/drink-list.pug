.container
  h4 Search for name, ingredients or directions
  form
    .form-group
      input.form-control(type="text", [value]="searchValue", (input)="searchValue=$event.target.value", placeholder="Type to search")


table.table.table-striped
    caption.text-center
        h3 Drinks
    tr
        th.pointer((click)="sort('id')") Id
        th.pointer((click)="sort('name')") Name
        th.pointer((click)="sort('ingredients')") Ingredients
        th.pointer((click)="sort('directions')") Directions
    tr(*ngFor='let drink of drinks | nameFilter:searchValue')
        td {{drink.id}}
        td {{drink.name}}
        td {{drink.ingredients}}
        td {{drink.directions}}
